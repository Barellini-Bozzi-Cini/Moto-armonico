#include <cmath>
#include "equation_solver.cpp"
auto posizione() {
	return A * cos(w * t);
	return a / -pow(w, 2);
}
auto tempo() {
	return acos(x / a) / w;
	return asin(v/(-A*w))/w
}
auto accelerazione() {
	return -A * pow(w, 2) * cos(w * t);
	return -x * pow(w, 2);
}
auto periodo() {
	return 1 / f;
	return 2 * pi / T;
}
auto velocit‡() {
	return -A * w * sin(w * t);
}
auto velocit‡Max() {
	return A * w;
}
auto accelerazioneMax() {
	return A * pow(w, 2);
}
auto pulsazione() {
	return acos(x / A) / t;
}
auto frequenza() {
	out <<
}
auto ampiezza() {
	out <<
}