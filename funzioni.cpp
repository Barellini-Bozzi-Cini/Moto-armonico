/*namespace prova {
	auto posizione() {
		return A * cos(w * t);
		return a / -pow(w, 2);
	}
	auto tempo() {
		return acos(x / A) / w;
		return asin(v / (-A * w)) / w;
	}
	auto accelerazione() {
		return -A * pow(w, 2) * cos(w * t);
		return -x * pow(w, 2);
	}
	auto periodo() {
		return 1 / f;
		return 2 * pi / w;
	}
	auto velocità() {
		return -A * w * sin(w * t);
	}
	auto velocitàMax() {
		return A * w;
	}
	auto accelerazioneMax() {
		return A * pow(w, 2);
	}
	auto pulsazione() {
		return acos(x / A) / t;
		return asin(v / (-A * w)) / t;
		return acos(a / (-A * pow(w, 2))) / t;
		return sqrt(a / -x);
		return sqrt(aMax / A);
		return vMax / A;
	}
	auto frequenza() {
		return 1 / T;
	}
	auto ampiezza() {
		return x / cos(w * t);
		return -v / (w * sin(w * t));
		return -a / (pow(w, 2) * cos(w * t));
		return aMax / pow(w, 2);
		return vMax / w;
	}
}
*/
